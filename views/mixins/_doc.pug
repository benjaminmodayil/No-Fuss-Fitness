mixin doc(head = {})
  doctype html
  html(lang="en").min-h-full
    head
      meta(name="viewport" content="width=device-width, initial-scale=1")
      title #{title}
      link(rel="stylesheet" href="/dist/style.css")
      link(href="/favicon.ico", rel="shortcut icon")
